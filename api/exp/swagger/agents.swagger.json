{
  "swagger": "2.0",
  "info": {
    "title": "agents.proto",
    "version": "version not set"
  },
  "schemes": [
    "http",
    "https"
  ],
  "consumes": [
    "application/json"
  ],
  "produces": [
    "application/json"
  ],
  "paths": {},
  "definitions": {
    "expAgent": {
      "type": "object",
      "properties": {
        "id": {
          "type": "integer",
          "format": "int32"
        },
        "facts": {
          "$ref": "#/definitions/expAgentFacts"
        },
        "group": {
          "type": "string"
        }
      }
    },
    "expAgentFacts": {
      "type": "object",
      "properties": {
        "hostname": {
          "type": "string"
        },
        "v4addr": {
          "type": "string",
          "description": "These are not set by the agent, but rather by the server on initial connection.",
          "title": "We store the incoming IP address (v4 and v6) of the requesting agent, as this will allow us to\nknow what to test against for servers"
        },
        "v6addr": {
          "type": "string"
        },
        "addresses": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "addresses is populated by the agent."
        }
      }
    },
    "expAgentList": {
      "type": "object",
      "properties": {
        "agents": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/expAgent"
          }
        }
      }
    },
    "expServerMessage": {
      "type": "object",
      "properties": {
        "id": {
          "type": "integer",
          "format": "int32"
        }
      }
    }
  }
}
