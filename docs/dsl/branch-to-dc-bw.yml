---
# Example test file
type: testrun
label: branch-to-dc-bw
spec:
    targettype: group
    source:
        name: branch-uraj
        app: iperf
        args: "-c {{ target }}"
    target:
        name: datacenter
        app: iperf
        args: "-s"
